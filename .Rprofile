if (interactive() && file.exists("use_renv.txt")) {
  source("renv/activate.R")
} else if (interactive()) {
  cat("This project uses renv for package management.\n")
  cat("To activate renv: create file 'use_renv.txt' and restart R\n")
  cat("To skip renv: just continue using your current packages\n")
}
